<%= render partial: 'users/user', locals: {user: @user} %>
<ul class="flex flex-col gap-y-2">
  <% @posts.each do |post| %>
    <%= render partial: 'post', locals: { post: post, comments: post.recent_comments } %>
  <% end %>
</ul>
